# M12 可测试PID核心

## 结论

通过。新增ROS无关的`lubanvision_control`包和单轴PID核心，RK ROS 2 Humble构建成功。

## 已验证语义

- 正归一化X误差默认产生负Pan增量，方向可显式设为`-1`或`+1`。
- 死区内目标输出为零且不继续积分。
- 积分进行双向对称限幅，限幅为0时禁用积分。
- PID目标输出和相邻周期输出变化分别限幅。
- 重置清除积分、微分历史和输出增量历史。
- 非有限误差以及非正或非有限时间步被拒绝。

## 测试结果

RK本包收集16项测试，16项通过；工作区测试结果汇总为27项测试、0错误、0失败、0跳过。
首次同步因macOS AppleDouble `._*`文件导致3项lint失败，清理元数据后回归通过，记录为
`ISSUE-20260716-13`。

## 下一任务

M13：实现目标丢失状态机，覆盖跟踪、短时保持、缓慢回中和重新捕获时PID重置。
